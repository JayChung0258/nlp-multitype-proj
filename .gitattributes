# Git attributes for NLP Multi-Type Classification Project

# Mark large model files as binary (do not attempt text diff)
*.bin binary
*.safetensors binary
*.pt binary
*.pth binary
*.ckpt binary
*.h5 binary
*.pkl binary
*.pickle binary
*.joblib binary

# Mark data files as binary
*.jsonl binary

# LFS tracking (if using Git LFS - uncomment these)
# *.bin filter=lfs diff=lfs merge=lfs -text
# *.safetensors filter=lfs diff=lfs merge=lfs -text
# *.joblib filter=lfs diff=lfs merge=lfs -text
